{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build:railway && cd backend && npm install"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
